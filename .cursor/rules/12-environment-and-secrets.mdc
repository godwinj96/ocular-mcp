---
description: Env var naming, per-package config validation, secret storage, what counts as a secret. Canonical source: docs/rules/12-environment-and-secrets.md
globs:
  - "packages/**/*"
alwaysApply: true
---

See `docs/rules/12-environment-and-secrets.md` for the full rule text. Do not duplicate content here.

Key points: validate required env vars at startup, fail loudly. `shared` never reads process.env. Secrets never committed, never logged, rotated on any suspected exposure. Every package ships a synced `.env.example`.
